@model IEnumerable<Domain.Entities.Pessoas.Profissoes.Profissao>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CBO)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CBO)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProfissaoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProfissaoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProfissaoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
