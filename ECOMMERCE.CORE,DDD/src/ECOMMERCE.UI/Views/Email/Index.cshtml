@model IEnumerable<Domain.Entities.Contatos.Email>

@{
    ViewData["Title"] = "Email";
}

<h2>Email</h2>

<p>
    <a class=" btn btn-success" asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EnderecoEmail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EnderecoEmail)
            </td>
            <td>
                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.EmailId">Editar</a> |
                <a class="btn btn-success" asp-action="Details" asp-route-id="@item.EmailId">Detalalhes</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.EmailId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
