@model IEnumerable<Domain.Entities.Contatos.TelefoneTipo>

@{
    ViewData["Title"] = "Telefone Tipo";
}

<h2>Telefone Tipo</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.TelefoneTipoId">Editar</a> |
                <a class="btn btn-success" asp-action="Details" asp-route-id="@item.TelefoneTipoId">Detalhes</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.TelefoneTipoId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
